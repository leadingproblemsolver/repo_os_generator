# Highest-Value Human Ownership Sprints

1. Reconstruct `generator.generate_repository` from its contract and tests; add a dry-run diff mode.
2. Diagnose a deliberately corrupted manifest and add integrity verification without mutating the target.
3. Explain and defend atomic staging, nonempty-target refusal, and force semantics.
4. Add one project-specific template extension while preserving deterministic output.
5. Containerize and demonstrate a clean checkout → install → generate → validate workflow.
