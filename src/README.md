---
    objective: Own runtime implementation code.
    why_it_exists: Every directory needs an explicit description so contributors never infer ownership from file names alone.
    how_to_use: Read this file before adding, moving, or deleting content inside this directory.
    dependencies: repository.yaml, INDEX.md
    owner: Builder
    update_policy: Update when this directory's responsibility or contents change.
    examples: A contributor checking `src/` can decide whether their work belongs here.
    related_files: INDEX.md, repository.yaml
    ---


# src/

## Responsibility

Own runtime implementation code.

## Boundary

Content belongs here only when it directly serves this responsibility. Related but separately owned material should link to the owning directory instead of duplicating it.
